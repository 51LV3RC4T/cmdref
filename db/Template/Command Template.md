Description :
     One-line summary shown in verbose search.

Parameters :
     Space-separated #argument names that appear as {{argument}} in the command (see variables.md).

```text
command with {{placeholders}} matching Parameters
```

```text
concrete example without placeholders
```

Tags :  #linux #topic

---

**Notes (not parsed by cmdref):**

- Separate each command entry with a line containing only `---`.
- Use **```cmd** / **```command** for the executable block (required).
- Use **```exp** or **```example** for the sample block.
- Placeholders in the command use double braces: `{{target-ip}}`, `{{attacker-port}}`, etc.
